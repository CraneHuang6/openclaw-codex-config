version = 1
id = "automation"
name = "小可每日巡检"
prompt = "使用 [$openclaw-update-workflow](/Users/crane/.codex/skills/openclaw-update-workflow/SKILL.md)。只执行本地脚本，不手工改代码。先运行 bash /Users/crane/.codex/scripts/check-agent-contracts.sh 做 contract precheck；若失败，按 CHECK_CATEGORY（contract/syntax/missing-file）归类为治理失败并立刻停止后续高成本流程。然后获取当前 LAN_IP（先运行 ipconfig getifaddr en0；若为空再运行 ipconfig getifaddr en1），再设置 OPENCLAW_SKILL_HTTP_PROXY_HOST=<LAN_IP>、OPENCLAW_SKILL_HTTP_PROXY_PORT=7897、OPENCLAW_SKILL_SOCKS_PROXY_PORT=7897、OPENCLAW_SKILL_PROXY_PRECHECK_ATTEMPTS=4、OPENCLAW_SKILL_PROXY_PRECHECK_RETRY_DELAY=2、OPENCLAW_SKILL_MONITOR_AUTO_FULL_ON_NEW_VERSION=0、OPENCLAW_NPM_REGISTRY=https://registry.npmjs.org，然后执行 bash /Users/crane/.codex/skills/openclaw-update-workflow/scripts/run_openclaw_update_flow.sh monitor。读取 REPORT_FILE 后按报告字段分类（infra/env/app），并检查 current_version 与 latest_version。若报告已标记 DNS/网络或环境限制（如 EPERM loopback），简短汇报并停止，不解释为小可功能故障，同时跳过 doctor。否则再执行 bash /Users/crane/.codex/skills/openclaw-update-workflow/scripts/run_openclaw_update_flow.sh doctor 做二次健康确认。若 latest_version 高于当前版本，只提示人工触发 full（bash /Users/crane/.codex/skills/openclaw-update-workflow/scripts/run_openclaw_update_flow.sh full），不要自动升级。最后用 bash /Users/crane/.codex/skills/openclaw-update-workflow/scripts/append-openclaw-update-memory.sh --report <REPORT_FILE> 追加自动化 memory。完成后仅简短汇报：当前版本、latest_version、dns_precheck、status --deep、gateway probe、security audit、feishu probe、doctor、报告路径；失败时给首个错误和下一步建议，并创建 inbox item。"
status = "PAUSED"
rrule = "FREQ=WEEKLY;BYDAY=MO,TU,WE,TH,FR,SA,SU;BYHOUR=4;BYMINUTE=0"
cwds = ["/Users/crane/.openclaw"]
created_at = 1771855729423
updated_at = 1771933327813
