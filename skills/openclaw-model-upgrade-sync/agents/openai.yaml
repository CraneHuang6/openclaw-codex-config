interface:
  display_name: "OpenClaw 模型升级同步"
  short_description: "大模型升级后执行一键总控：静态同步、运行态硬切、探针验模、防漂移"
  default_prompt: "Use $openclaw-model-upgrade-sync to run the one-shot model switch flow: static sync, runtime hard-cut, baseline probe sessions, and drift verification, while preserving monitor on mini."
