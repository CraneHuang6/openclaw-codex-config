interface:
  display_name: "Merge Manager"
  short_description: "盘点、分类、验证并生成本地分支 dry-run 集成报告"
  default_prompt: "Use $merge-manager to scan local branches, classify risk, validate dry-run integration readiness, and render a merge planning report without executing real merges."
